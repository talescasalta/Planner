// Placeholder until supabase gen types is run against a real database.
// After creating the Supabase project and applying migrations, generate types with:
//   npx supabase gen types typescript --project-id <project-id> --schema public > src/lib/types/database.ts
// eslint-disable-next-line @typescript-eslint/no-explicit-any -- replaced by generated Supabase types when a project is linked.
export type Database = any;
